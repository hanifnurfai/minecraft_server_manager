[project]
name = "minecraft-server-manager"
version = "0.1.0"
description = "a simple project to manage minecraft server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "fastapi[standard]>=0.128.0",
    "requests>=2.32.5",
]
